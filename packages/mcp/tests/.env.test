# Test environment configuration for MCP E2E tests
# Copy this to .env.test.local and adjust as needed

# Listmonk server configuration
LISTMONK_URL=http://localhost:9000
LISTMONK_USERNAME=admin
LISTMONK_PASSWORD=adminpass
LISTMONK_API_TOKEN=

# Test configuration
TEST_TIMEOUT=30000
TEST_CLEANUP=true

# Docker configuration (if using Docker for tests)
DOCKER_COMPOSE_FILE=docker-compose.test.yml
DOCKER_PROJECT_NAME=listmonk-mcp-test