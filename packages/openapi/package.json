{
	"name": "@listmonk-ops/openapi",
	"version": "0.1.1",
	"module": "index.ts",
	"types": "index.ts",
	"type": "module",
	"scripts": {
		"generate": "bun run generate.ts",
		"test": "bun test",
		"test:watch": "bun test --watch",
		"build": "tsc && esbuild index.ts --bundle --outfile=dist/index.js --format=esm --minify"
	},
	"devDependencies": {
		"@hey-api/openapi-ts": "0.93.1",
		"@types/bun": "1.3.10",
		"esbuild": "0.27.3"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
