---
npm/@listmonk-ops/operations: patch
---

Keep generic and named template and user-role manifest invocation paths consistent. Both paths now enforce the raw 1 MiB payload limit before remote reads and preserve body-free typed partial-apply errors.
